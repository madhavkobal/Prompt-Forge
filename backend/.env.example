# =============================================================================
# PromptForge Backend Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# Do NOT commit the .env file to version control!
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database connection string
# Format: postgresql://username:password@host:port/database_name
#
# For local development:
DATABASE_URL=postgresql://promptforge:promptforge_dev_password@localhost:5432/promptforge_db
#
# For Docker Compose:
# DATABASE_URL=postgresql://promptforge:promptforge_dev_password@postgres:5432/promptforge_db
#
# For production (use managed PostgreSQL service):
# DATABASE_URL=postgresql://user:password@your-postgres-host:5432/promptforge_prod
#
# Important: Create the database before running the application:
#   createdb promptforge_db
#   OR use Docker Compose which creates it automatically

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# JWT Secret Key - CRITICAL: Change this to a strong random string in production!
# Generate a secure key with: openssl rand -hex 32
# Or: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production-use-openssl-rand-hex-32

# JWT Algorithm - DO NOT CHANGE unless you know what you're doing
ALGORITHM=HS256

# Access Token Expiration (in minutes)
# Default: 10080 minutes = 7 days
# For production, consider shorter expiration (e.g., 60 for 1 hour)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# =============================================================================
# GOOGLE GEMINI API CONFIGURATION
# =============================================================================
# Google Gemini API Key - REQUIRED for AI features
# Get your API key from: https://makersuite.google.com/app/apikey
# Free tier: 60 requests per minute
#
GEMINI_API_KEY=your-gemini-api-key-here
#
# Important:
# - Keep this key secure and never commit it to version control
# - Monitor your API usage at https://makersuite.google.com
# - Consider rate limiting in production

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Include all frontend URLs that will access this API
#
# For local development (Vite default port):
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
#
# For production, add your production frontend URL:
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
#
# For development with multiple frontends:
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Environment: development, staging, production
ENVIRONMENT=development

# API Version (used in URL paths: /api/v1)
# Generally, you should not change this
API_V1_STR=/api/v1

# Project Name
PROJECT_NAME=PromptForge

# Application Version
VERSION=1.0.0

# =============================================================================
# OPTIONAL: ADVANCED SETTINGS
# =============================================================================

# Server Host and Port (if running with uvicorn directly)
# HOST=0.0.0.0
# PORT=8000

# Database Pool Size (for high-traffic production environments)
# DB_POOL_SIZE=20
# DB_MAX_OVERFLOW=0

# Logging Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Sentry DSN for error tracking (production)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Redis URL for caching (if implementing cache)
# REDIS_URL=redis://localhost:6379/0

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
# ✓ SECRET_KEY is changed to a strong random value
# ✓ DATABASE_URL points to production database with strong password
# ✓ GEMINI_API_KEY is valid and has sufficient quota
# ✓ CORS_ORIGINS includes only your production frontend domain(s)
# ✓ ENVIRONMENT is set to "production"
# ✓ ACCESS_TOKEN_EXPIRE_MINUTES is set appropriately for security
# ✓ SSL/TLS is enabled (HTTPS)
# ✓ Database backups are configured
# ✓ API rate limiting is implemented
# ✓ Monitoring and logging are set up
# =============================================================================
