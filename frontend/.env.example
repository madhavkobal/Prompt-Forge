# =============================================================================
# PromptForge Frontend Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# Note: Vite requires environment variables to be prefixed with VITE_
# =============================================================================

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API URL
# This is where your FastAPI backend is running
#
# For local development with backend on default port:
VITE_API_URL=http://localhost:8000
#
# For Docker Compose (backend service name):
# VITE_API_URL=http://backend:8000
#
# For production (use your production API domain):
# VITE_API_URL=https://api.yourdomain.com
#
# Important:
# - Do NOT include /api/v1 in this URL (it's added automatically)
# - Make sure this URL is accessible from the browser (not just from server)
# - For Docker, use the service name from docker-compose.yml
# - For production, use HTTPS

# =============================================================================
# APPLICATION SETTINGS (OPTIONAL)
# =============================================================================

# Application Name
# VITE_APP_NAME=PromptForge

# Application Version
# VITE_APP_VERSION=1.0.0

# Enable Debug Mode (shows console logs)
# VITE_DEBUG=false

# =============================================================================
# ANALYTICS (OPTIONAL)
# =============================================================================

# Google Analytics Measurement ID
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking (production)
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# =============================================================================
# FEATURE FLAGS (OPTIONAL)
# =============================================================================

# Enable experimental features
# VITE_ENABLE_EXPERIMENTAL=false

# Enable dark mode toggle
# VITE_ENABLE_DARK_MODE=true

# =============================================================================
# NOTES
# =============================================================================
# - All variables must be prefixed with VITE_ to be exposed to the browser
# - Changes to .env require restarting the dev server (npm run dev)
# - Environment variables are embedded at build time (not runtime)
# - For runtime configuration, use a config API endpoint instead
# - Never store sensitive secrets in frontend .env (they're public in browser)
# =============================================================================

# =============================================================================
# DEVELOPMENT vs PRODUCTION
# =============================================================================
# Development (.env.development):
#   VITE_API_URL=http://localhost:8000
#
# Production (.env.production):
#   VITE_API_URL=https://api.yourdomain.com
#
# You can create separate .env files for different environments:
# - .env.local (overrides .env, not committed to git)
# - .env.development (development mode)
# - .env.production (production build)
# =============================================================================
